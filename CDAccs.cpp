#include <vector>
#include <iostream>
#include <algorithm>
#include <string>
#include <fstream>



using namespace std;
using std::cin;
using std::cout;
using std::endl;
using std::vector;
using std::string;
using std::getline;

int CDTotal = 0;
int newesttotal = 0;

// if Cash deposit, account, it will take the current variable initially set at 0 and add the amount of the checking account to it
while (readfile.good())
	{
		newesttotal = amount;
		CDTotal = CDTotal + newesttotal;
	}
